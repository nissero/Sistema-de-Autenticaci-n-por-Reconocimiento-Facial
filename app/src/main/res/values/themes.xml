<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="CloudChipOption" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipStrokeColor">@color/blue</item>
        <item name="chipMinHeight">48dp</item>
        <item name="chipStrokeWidth">2dp</item>
        <item name="android:textColor">@color/chip_text_color_list</item>
        <item name="chipBackgroundColor">@color/chip_background_color_list</item>

    </style>
</resources>